import { group, sleep } from 'k6'
import { accepted, rpc } from './helpers.js'
export const options = { scenarios: { event: { executor: 'ramping-vus', startVUs: 10, stages: [{ target: 50, duration: '15s' }, { target: 100, duration: '15s' }, { target: 200, duration: '15s' }, { target: 300, duration: '15s' }, { target: 500, duration: '20s' }, { target: 0, duration: '10s' }] } }, thresholds: { http_req_failed: ['rate<0.01'], http_req_duration: ['p(95)<500'] } }
export default function () { group('state scheduling', () => { sleep(6 + Math.random() * 4) }); if (__ENV.PERFECT_ROUND_ID) group('Perfect Second', () => accepted(rpc('submit_perfect_second', { p_session_token: __ENV['TOKEN_' + __VU], p_round_id: __ENV.PERFECT_ROUND_ID, p_elapsed_ms: 6000 + Math.floor(Math.random() * 400) - 200, p_timing_metadata: { load_test: true } }), 'Perfect Second')); sleep(1 + Math.random() * 2); if (__ENV.FIRST_LOOK_ROUND_ID) group('First Look', () => accepted(rpc('submit_first_look', { p_session_token: __ENV['TOKEN_' + __VU], p_round_id: __ENV.FIRST_LOOK_ROUND_ID, p_guess: 20 + Math.floor(Math.random() * 41) }), 'First Look')) }
